'use strict';

describe('myApp.cluster module', function() {

  beforeEach(module('myApp.cluster'));

  describe('cluster controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clusterCtrl = $controller('ClusterCtrl');
      expect(clusterCtrl).toBeDefined();
    }));

  });
});